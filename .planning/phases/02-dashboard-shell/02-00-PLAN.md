---
phase: 02-dashboard-shell
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/dashboard/agent-picker.test.ts
  - src/__tests__/dashboard/profile-fetch.test.ts
  - src/__tests__/dashboard/economy-context.test.ts
  - src/__tests__/dashboard/daily-claim.test.ts
  - src/__tests__/dashboard/start-quest.test.ts
  - src/__tests__/dashboard/agent-persist.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Six test stub files exist in src/__tests__/dashboard/ and pass (todos) when run"
    - "Each file imports the correct helper from src/__tests__/helpers/supabase-mock.ts"
    - "npx vitest run src/__tests__/dashboard/ exits 0 (all stubs are todos, not failures)"
  artifacts:
    - path: "src/__tests__/dashboard/agent-picker.test.ts"
      provides: "Test stubs for DASH-01 (agent picker renders 4 agents)"
    - path: "src/__tests__/dashboard/profile-fetch.test.ts"
      provides: "Test stubs for DASH-02 (profile fetch returns gold/xp/energy/level)"
    - path: "src/__tests__/dashboard/economy-context.test.ts"
      provides: "Test stubs for DASH-03 (EconomyContext initializes from props)"
    - path: "src/__tests__/dashboard/daily-claim.test.ts"
      provides: "Test stubs for DASH-04 (daily claim calls awardLoot)"
    - path: "src/__tests__/dashboard/start-quest.test.ts"
      provides: "Test stubs for DASH-05 (start quest calls spendEnergy)"
    - path: "src/__tests__/dashboard/agent-persist.test.ts"
      provides: "Test stubs for DASH-06 (saveAgentSelection writes agent_id to Supabase)"
  key_links:
    - from: "src/__tests__/dashboard/*.test.ts"
      to: "src/__tests__/helpers/supabase-mock.ts"
      via: "import { mockSupabaseClient, mockSupabaseAdmin }"
      pattern: "mockSupabase"
---

<objective>
Create Wave 0 test stub files for all six dashboard requirements. These are failing-ready stubs (using it.todo) that will be filled in as implementation plans complete.

Purpose: Satisfy Nyquist rule — every automated verify command in Plans 01–07 must have a corresponding test file. Without these stubs, later plans cannot reference automated verify commands.
Output: Six test files in src/__tests__/dashboard/ that run clean (0 failures, all todos).
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-shell/02-VALIDATION.md

Existing test pattern reference — src/__tests__/auth/pin-login.test.ts uses:
```typescript
import { mockSupabaseClient } from "../helpers/supabase-mock";
describe("AUTH-04: PIN Login", () => {
  it.todo("authenticates with correct PIN");
});
```

Existing mock at src/__tests__/helpers/supabase-mock.ts exports:
- mockSupabaseClient() — for client-side Supabase (auth operations)
- mockSupabaseAdmin() — for admin/service-role writes (economy, agent persistence)
The admin mock's fromChain does NOT yet include rpc() — add it in daily-claim and start-quest stubs by extending the mock inline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for DASH-01, DASH-02, DASH-03</name>
  <files>
    src/__tests__/dashboard/agent-picker.test.ts,
    src/__tests__/dashboard/profile-fetch.test.ts,
    src/__tests__/dashboard/economy-context.test.ts
  </files>
  <action>
Create the dashboard test directory and three stub files. Use the exact same pattern as src/__tests__/auth/pin-login.test.ts (import helper, describe with req ID in name, it.todo entries).

src/__tests__/dashboard/agent-picker.test.ts:
```typescript
import { mockSupabaseClient } from "../helpers/supabase-mock";

describe("DASH-01: Agent Picker", () => {
  it.todo("renders all 4 agents: Cooper, Arlo, Minh, Maya");
  it.todo("calls saveAgentSelection with correct agentId on card click");
  it.todo("triggers portal-warp transition (isPending state) on selection");
});
```

src/__tests__/dashboard/profile-fetch.test.ts:
```typescript
import { mockSupabaseClient } from "../helpers/supabase-mock";

describe("DASH-02: Profile Fetch", () => {
  it.todo("returns gold, xp, energy, level from profiles row");
  it.todo("redirects to / when user is not authenticated");
  it.todo("returns default values (gold:0, xp:0, energy:100, level:1) when profile row is null");
});
```

src/__tests__/dashboard/economy-context.test.ts:
```typescript
describe("DASH-03: EconomyContext", () => {
  it.todo("initializes gold from initialGold prop without client fetch");
  it.todo("initializes xp from initialXp prop without client fetch");
  it.todo("initializes energy from initialEnergy prop without client fetch");
  it.todo("setGold updates gold value");
  it.todo("setEnergy updates energy value");
  it.todo("useEconomy throws if called outside EconomyProvider");
});
```

Note: economy-context.test.ts does NOT need supabase-mock import — EconomyContext is pure React state.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/dashboard/agent-picker.test.ts src/__tests__/dashboard/profile-fetch.test.ts src/__tests__/dashboard/economy-context.test.ts</automated>
  </verify>
  <done>Three test files exist, vitest reports 0 failures (all tests are todos), exit code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for DASH-04, DASH-05, DASH-06</name>
  <files>
    src/__tests__/dashboard/daily-claim.test.ts,
    src/__tests__/dashboard/start-quest.test.ts,
    src/__tests__/dashboard/agent-persist.test.ts
  </files>
  <action>
Create three more stub files. DASH-04 and DASH-05 involve economy Server Actions that call supabaseAdmin.rpc() — include inline rpc mock setup in the describe block comment so implementors know the mock needs extending.

src/__tests__/dashboard/daily-claim.test.ts:
```typescript
import { mockSupabaseClient, mockSupabaseAdmin } from "../helpers/supabase-mock";

// Note: mockSupabaseAdmin().from()._fromChain does not have rpc().
// When implementing, add: vi.mock("@/lib/supabase/admin", ...) with rpc mock.
describe("DASH-04: Daily Claim", () => {
  it.todo("calls awardLoot(25, 'daily_bonus') on button click");
  it.todo("updates gold in EconomyContext on success");
  it.todo("does not update gold on awardLoot failure");
});
```

src/__tests__/dashboard/start-quest.test.ts:
```typescript
import { mockSupabaseClient, mockSupabaseAdmin } from "../helpers/supabase-mock";

// Note: mockSupabaseAdmin().from()._fromChain does not have rpc().
// When implementing, add: vi.mock("@/lib/supabase/admin", ...) with rpc mock.
describe("DASH-05: Start Quest Button", () => {
  it.todo("calls spendEnergy(10, 'training_quest') on button click");
  it.todo("updates energy in EconomyContext on success");
  it.todo("shows insufficient energy message when spendEnergy fails");
});
```

src/__tests__/dashboard/agent-persist.test.ts:
```typescript
import { mockSupabaseClient, mockSupabaseAdmin } from "../helpers/supabase-mock";

describe("DASH-06: Agent Persistence", () => {
  it.todo("saveAgentSelection writes agent_id to profiles table via supabaseAdmin");
  it.todo("saveAgentSelection returns { success: false } when user not authenticated");
  it.todo("saveAgentSelection returns { success: false } on Supabase write error");
  it.todo("saveAgentSelection rejects invalid agentId values outside the 4 known agents");
});
```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/dashboard/</automated>
  </verify>
  <done>All six dashboard test files exist. npx vitest run src/__tests__/dashboard/ reports 0 failures, all tests in todo state, exit code 0.</done>
</task>

</tasks>

<verification>
Run: npx vitest run src/__tests__/dashboard/
Expected: 0 failures, 0 errors, all tests are todo stubs, exit 0.
</verification>

<success_criteria>
Six files in src/__tests__/dashboard/ exist. npx vitest run src/__tests__/dashboard/ exits 0 with all tests in todo state. Each file maps to one DASH requirement and imports from the established mock helper.
</success_criteria>

<output>
After completion, create .planning/phases/02-dashboard-shell/02-00-SUMMARY.md
</output>
