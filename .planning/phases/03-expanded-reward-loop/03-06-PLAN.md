---
phase: 03-expanded-reward-loop
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MissionCompleteOverlay.tsx
autonomous: true
gap_closure: true
requirements: [ECON-05]

must_haves:
  truths:
    - "Mission completion triggers a visible confetti/particle burst celebration effect"
    - "Confetti renders on overlay mount, before user clicks Collect Rewards"
  artifacts:
    - path: "src/components/MissionCompleteOverlay.tsx"
      provides: "Confetti particle burst on mission complete"
      contains: "confetti"
  key_links:
    - from: "MissionCompleteOverlay.tsx"
      to: "motion/react"
      via: "Framer Motion animate for particle elements"
      pattern: "motion\\."
---

<objective>
Add a confetti/particle burst celebration effect to MissionCompleteOverlay.

Purpose: ECON-05 requires "particle burst / confetti" on quest completion. The overlay currently has scale/opacity animations but no celebratory particle effect.
Output: MissionCompleteOverlay renders a burst of animated confetti particles when the overlay appears.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/MissionCompleteOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confetti particle burst to MissionCompleteOverlay</name>
  <files>src/components/MissionCompleteOverlay.tsx</files>
  <action>
Add a confetti/particle burst effect to MissionCompleteOverlay that fires when the overlay mounts. Implementation approach:

1. Create a `ConfettiBurst` component inside MissionCompleteOverlay.tsx (no new file needed — keeps it self-contained).
2. Generate 30-40 confetti particle elements using Framer Motion `motion.div`.
3. Each particle should:
   - Start from the center-top of the card (near the success badge)
   - Animate outward in random directions (use Math.random for x spread: -200 to +200, y spread: -100 to +300)
   - Have random rotation (0 to 720deg)
   - Use phase-appropriate colors: #10B981 (green), #3B82F6 (blue), #F59E0B (gold), #8B5CF6 (violet)
   - Be small rectangles (6-10px wide, 4-8px tall) with rounded corners
   - Fade out over 1.5-2.5 seconds (randomized per particle)
   - Use `initial` + `animate` with random `transition.duration` and `transition.delay` (stagger 0-0.3s)
4. Render `ConfettiBurst` inside the overlay container, positioned absolutely, after the backdrop div and before the card.
5. Respect `prefers-reduced-motion`: wrap in a check — if reduced motion preferred, skip rendering confetti entirely. Use `window.matchMedia('(prefers-reduced-motion: reduce)')` in a useMemo or useState+useEffect.
6. Import `motion` from `motion/react` (already imported in the file).
7. Do NOT use any external confetti library — pure Framer Motion.
8. The confetti should be pointer-events-none so it doesn't block the card interaction.

Keep existing overlay animations (scale, opacity on card/badge/rewards) exactly as they are. The confetti is additive — layered behind the card (z-index below the card's z-10).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>MissionCompleteOverlay renders 30-40 animated confetti particles on mount. Particles spread outward from center with randomized trajectories, rotations, and colors (#10B981, #3B82F6, #F59E0B, #8B5CF6). Particles fade out over 1.5-2.5s. Reduced motion users see no confetti. Existing overlay animations unchanged.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (no regressions)
- MissionCompleteOverlay.tsx contains confetti-related elements with Framer Motion animation props
- Confetti elements use pointer-events-none
- prefers-reduced-motion check exists
</verification>

<success_criteria>
ECON-05 satisfied: Quest completion triggers a Framer Motion celebration with particle burst / confetti effect in MissionCompleteOverlay.
</success_criteria>

<output>
After completion, create `.planning/phases/03-expanded-reward-loop/03-06-SUMMARY.md`
</output>
