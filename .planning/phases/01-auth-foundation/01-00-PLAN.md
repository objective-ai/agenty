---
phase: 01-auth-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/__tests__/helpers/supabase-mock.ts
  - src/__tests__/auth/magic-link.test.ts
  - src/__tests__/auth/callback.test.ts
  - src/__tests__/auth/setup.test.ts
  - src/__tests__/auth/pin-login.test.ts
  - src/__tests__/auth/rate-limit.test.ts
  - src/__tests__/auth/middleware.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "npx vitest run executes without config errors"
    - "All 6 test stub files exist and contain at least one skipped or placeholder test"
    - "Supabase mock helper exports reusable mock factories"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with Next.js path aliases"
    - path: "src/__tests__/helpers/supabase-mock.ts"
      provides: "Shared mock factories for Supabase client, admin, and auth"
      exports: ["mockSupabaseClient", "mockSupabaseAdmin"]
    - path: "src/__tests__/auth/magic-link.test.ts"
      provides: "Test stubs for AUTH-01 sendMagicLink"
    - path: "src/__tests__/auth/callback.test.ts"
      provides: "Test stubs for AUTH-02 PKCE callback"
    - path: "src/__tests__/auth/setup.test.ts"
      provides: "Test stubs for AUTH-03 setupChildAccount"
    - path: "src/__tests__/auth/pin-login.test.ts"
      provides: "Test stubs for AUTH-04 loginWithPin"
    - path: "src/__tests__/auth/rate-limit.test.ts"
      provides: "Test stubs for AUTH-05 rate limiting"
    - path: "src/__tests__/auth/middleware.test.ts"
      provides: "Test stubs for AUTH-06 middleware redirect"
  key_links:
    - from: "src/__tests__/auth/*.test.ts"
      to: "src/__tests__/helpers/supabase-mock.ts"
      via: "import mock factories"
      pattern: "import.*from.*helpers/supabase-mock"
---

<objective>
Install Vitest and create test infrastructure with stub test files for all auth requirements.

Purpose: Wave 0 — establish the test framework and stub files so that all subsequent plans can include vitest commands in their verify blocks. Without this, no automated testing is possible.

Output: Working vitest config, shared mock helpers, and 6 stub test files (one per auth requirement).
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md
@.planning/phases/01-auth-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create config</name>
  <files>vitest.config.ts</files>
  <action>
Install Vitest and create configuration:

```bash
npm install -D vitest @vitejs/plugin-react
```

Create `vitest.config.ts` with:
- `@vitejs/plugin-react` plugin
- Path alias: `@` maps to `./src` (matching tsconfig.json)
- Environment: `node` (server actions are server-side)
- Include pattern: `src/__tests__/**/*.test.ts`
- Globals: true (so `describe`, `it`, `expect` are available without imports)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>vitest.config.ts exists, `npx vitest run` executes without config errors (may show "no tests" which is fine at this point)</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase mock helpers and 6 test stub files</name>
  <files>src/__tests__/helpers/supabase-mock.ts, src/__tests__/auth/magic-link.test.ts, src/__tests__/auth/callback.test.ts, src/__tests__/auth/setup.test.ts, src/__tests__/auth/pin-login.test.ts, src/__tests__/auth/rate-limit.test.ts, src/__tests__/auth/middleware.test.ts</files>
  <action>
**src/__tests__/helpers/supabase-mock.ts** — Shared mock factories:
- `mockSupabaseClient()` — returns a mock with `auth.signInWithOtp`, `auth.signInWithPassword`, `auth.verifyOtp`, `auth.getUser`, `auth.getClaims`, `auth.signOut`, and `from().select/insert/upsert/delete` chain
- `mockSupabaseAdmin()` — returns a mock with `auth.admin.createUser`, `auth.admin.listUsers`, `auth.admin.getUserByEmail`, and `from()` chain
- Use `vi.fn()` for all methods so tests can set return values with `.mockResolvedValue()`

**6 test stub files** — Each file gets a `describe` block with `it.todo()` entries describing the behaviors to test. These are placeholders that subsequent plans will fill in:

`magic-link.test.ts` (AUTH-01):
- `it.todo("sends magic link with correct emailRedirectTo")`
- `it.todo("returns error for invalid email")`

`callback.test.ts` (AUTH-02):
- `it.todo("verifies OTP and redirects to /setup on success")`
- `it.todo("redirects to /auth/error on invalid token")`
- `it.todo("redirects to /auth/error when token_hash missing")`

`setup.test.ts` (AUTH-03):
- `it.todo("creates kid auth user with synthetic email and PIN")`
- `it.todo("upserts profile row for kid")`
- `it.todo("rejects unauthenticated caller")`
- `it.todo("validates PIN is exactly 6 digits")`

`pin-login.test.ts` (AUTH-04):
- `it.todo("authenticates with correct PIN")`
- `it.todo("returns wrong_pin for incorrect PIN")`
- `it.todo("clears attempts on successful login")`

`rate-limit.test.ts` (AUTH-05):
- `it.todo("allows login when under attempt limit")`
- `it.todo("returns tactical_lockdown after 5 failed attempts")`
- `it.todo("includes unlockAt and remainingSeconds in lockout response")`

`middleware.test.ts` (AUTH-06):
- `it.todo("redirects /bridge to / when no session")`
- `it.todo("redirects / to /bridge when session exists")`
- `it.todo("allows /setup through regardless of auth state")`

Each file imports from `../helpers/supabase-mock` even though stubs don't use mocks yet — this validates the import path works.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- All 6 test files exist under src/__tests__/auth/
- supabase-mock.ts exports mockSupabaseClient and mockSupabaseAdmin
- `npx vitest run` shows all .todo tests as skipped (no failures)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` completes with 0 failures
- All test files are discovered by vitest
- Mock helper imports resolve correctly
</verification>

<success_criteria>
- Vitest installed and configured with path aliases
- 6 test stub files with .todo entries for all auth requirements
- Shared Supabase mock helper ready for use
- `npx vitest run` is a valid command for all subsequent plan verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-00-SUMMARY.md`
</output>
