import { mockSupabaseClient } from "../helpers/supabase-mock";

describe("AUTH-01: Magic Link", () => {
  it.todo("sends magic link with correct emailRedirectTo");
  it.todo("returns error for invalid email");
});
